version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: mysql-autoescola
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: autoescolan116
    ports:
      - "3306:3306"
